services:
  - type: web
    name: url-shortener
    runtime: docker
    plan: free
    healthCheckPath: /
    preDeployCommand: python manage.py migrate
    envVars:
      - key: DATABASE_URL
        sync: false
